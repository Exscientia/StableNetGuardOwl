name: test
channels:
  - conda-forge

  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - openmm>=8.0
  - openmm-ml
  - openmm-torch
  - openff-toolkit
  - openmmtools
  - nnpops>=0.4
  - cudatoolkit
  - tqdm
  - mdtraj
  - pymbar>=4.0
  - torchani
  - seaborn
  - pytorch=1.11.0
  - loguru
  # Testing
  - nglview
  - fire

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
      - nvidia-ml-py3
      - nptyping
